import webix from "webix";

export default () => {

    
}

export function router(url) {
    /**
     * pretty AJAX URL
     * S.http://domain:port/index.html?param=XXX#!iscp.oss.index/openbook.index
     * 
     */
    console.log(url);

    
    
}